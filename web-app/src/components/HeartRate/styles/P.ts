import styled from 'styled-components';

export const P = styled.p`
  position: absolute;
  transform: translate(-50%, -50%);
  left: 50%;
  top: 200px;
  margin: 0;
  font-weight: bold;
  font-size: 10rem;
  color: white;
`;
